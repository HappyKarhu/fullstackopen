import ReactDOM from 'react-dom/client' //Imports the ReactDOM library for rendering React apps

import App from './App' // Imports main App component

import './index.css' // Imports CSS file globally

ReactDOM.createRoot(document.getElementById('root')).render(<App />)